# .goreleaser.yml
# Build customization
builds:
  - main: main.go
    binary: github-enterprise-replication-exporter
    goos:
      - windows
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
# Archive customization
archive:
  format: tar.gz